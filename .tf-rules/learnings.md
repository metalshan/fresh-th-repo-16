## Consolidated

## Recent
[chat] Button block style_class valid values are: "button" (primary), "button-secondary" (secondary), "link" (text link). Do NOT use "button-primary" — it's invalid and renders as unstyled text.
